# URECA Result Templates

These directories are placeholders for real experiment outputs produced on Linux.
Each example creates its `summary.csv`, `trajectories/`, and `figures/` directories at runtime.
